cache=.npm-cache

# Vercel uses npm with strict peer dependency resolution.
# Next.js canary versions (e.g. 16.x.x-canary) are pre-releases and may not satisfy
# peer ranges like ">= 13" unless the range explicitly includes pre-releases.
# This keeps installs deterministic in CI/Vercel.
legacy-peer-deps=true
